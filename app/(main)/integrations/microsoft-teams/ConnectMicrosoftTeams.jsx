'use client'

import { useEffect, useState } from 'react'
import { useSearchParams } from 'next/navigation'
// ... other imports ...

export default function ConnectMicrosoftTeams() {
  // ... paste all the existing component logic here ...
  // (Keep the original return statement and all hooks)
} 