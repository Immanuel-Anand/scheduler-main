/** @type {import('next').NextConfig} */
const nextConfig = {
    // Disable turbopack for now
    experimental: {
        // turbopack: {
        //     enabled: false
        // }
    }
}

export default nextConfig;
